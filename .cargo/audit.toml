# cargo-audit configuration. Vulnerabilities always fail; the entries below
# are informational (unmaintained) advisories on transitive crates that we
# have looked at and decided to carry. Revisit an entry only if a real
# security advisory appears against the crate.
[advisories]
ignore = [
    # bincode 1.3 via cdshealpix's HEALPix sort module (starfield-gaia).
    # Works, unmaintained notice only; decision 2026-09-15 to keep it.
    "RUSTSEC-2025-0141",
    # paste via simba 0.8 (nalgebra 0.32) and rav1e (image avif). Clearing
    # it is a coordinated nalgebra 0.35 bump across starfield,
    # cfl-foundations and focalplane: CosmicFrontierLabs/cfl-foundations#7.
    "RUSTSEC-2024-0436",
    # number_prefix via indicatif 0.17 in the pinned cfl-foundations rev;
    # gone once focalplane picks up a cfl-foundations rev with #42.
    "RUSTSEC-2025-0119",
]
